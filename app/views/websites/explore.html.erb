
<section class="userHeader">
  <h2 class="red">Explore The Latest Bookmarks</h2>
  <p>Explore the latest bookmarks added. A live collection of the most recently added bookmarks to markker.</p>
</section>

<div class="card">
  <div class="image_gallery">
  <% @websites.reverse.each do |website| %>
  <div class="bookmark_img">
    <%= link_to image_tag(website.image, :class => 'thumb', :alt => website.title), website %>
  </div>
  <% end %>
  </div>
</div>
